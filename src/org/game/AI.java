package org.game;
import java.awt.Graphics;


public class AI {
/*	
	
	diceNumber : Board.getDiceNumber();
	boolean cantMove = false;
	checkPosition : Board.checkPosition(f, diceNumber, cantMove);
	rollDice : Board.rollDice();
	for (Figure f : player.getFigures());{
		currentXY : Board.tick(currentXY);
	if (currentXY == f.getPosition()){
		
		if (diceNumber == 6){
			if (f.getPosition() == -1){
				
				checkPosition(f, 0, cantMove);
				
				rollDice = false;
			}
			else if (f.getPosition() < 30){	
				checkPosition(f, diceNumber, cantMove);
				rollDice = false;
			}
			else {
				for (Figure fig : player.getFigures()){
					player :player.getFigures();
					if (f.getPosition() + diceNumber == fig.getPosition()){
						break;
					}
					else {
						f.setMove(diceNumber);
						checkWin(player);
						rollDice = false;
					}
				}
			}
		}
		else if (f.getPosition() != -1){
			checkPosition(f, diceNumber, cantMove);
			checkWin(player);
		}
		else {
			rollDice = true;
			moved = true;
		}
	}
	
	}

}	*/
}
